# SYNDICATE v14 fully checked fixed

Проверено и исправлено:
- Python-файлы проходят `py_compile`;
- frontend проходит `npm run build`;
- `/` отдаёт собранное приложение, `/api/health` отдаёт JSON;
- в корзине только Самовывоз и МЦД-3;
- Самовывоз = 0 ₽;
- МЦД-3 = 100 ₽;
- стоимость получения входит в итог;
- для самовывоза отдельные кнопки: Ипподром, Раменское, Фабричная, Есенинская, Кратово, Отдых;
- для МЦД-3 покупатель сам пишет станцию;
- кнопка SYNDICATE убрана из клавиатур;
- кнопка Назад сбрасывает состояние;
- база сохраняется при подключении `/data` и `DB_PATH=/data/bot.db`.

Что исправлено:
1. Исправлен баг старых вариантов без остатков: старый формат вариантов больше не раздувает общий склад при подтверждении заказа.
2. /reset_user_test теперь откатывает влияние удаляемых bonus_transactions на балансы других пользователей, включая реферальный бонус пригласившего.
3. Slash-команды разделены: обычные пользователи видят только /start, /menu, /id; админские команды назначаются только chat_id админов.
4. Бэкап SQLite теперь создаётся через sqlite backup API, а не простым copy2 файла базы; это корректно учитывает WAL.
5. Сообщение покупателю после оформления заказа теперь показывает промокод и сумму скидки.
6. Добавлена подготовка к persistent storage: DB по умолчанию /data/bot.db, uploads по умолчанию /data/uploads, Dockerfile объявляет VOLUME /data. При первом запуске старые /app/bot.db и /app/data/uploads пробуют перенестись.
7. Промокод теперь считается использованным только после подтверждения заказа админом.
8. Отмена неподтверждённого заказа больше не уменьшает used_count промокода.
9. В админку добавлена кнопка 📊 Статистика.
10. Добавлена команда /stats.
11. В статистике показывается количество пользователей, авторизованных в боте, новые пользователи за сегодня/7/30 дней, заказы, выручка, средний чек, товары и категории.
12. DEBUG_TOKEN больше не принимается из query-параметра token, только через header X-Debug-Token.

Важно для Dockhost:
- Нужно подключить persistent volume к /data.
- После деплоя проверь, что DB_PATH=/data/bot.db и UPLOAD_DIR=/data/uploads или оставь новые defaults.

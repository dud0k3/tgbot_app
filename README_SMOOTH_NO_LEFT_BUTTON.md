# SYNDICATE TG App v12 — плавность + без кнопки молнии

Изменено:
- убрана кнопка-молния слева сверху;
- центрирование логотипа сохранено через невидимый spacer;
- добавлены плавные переходы экранов;
- добавлены мягкие нажатия на карточки и кнопки;
- добавлена плавная анимация кнопки `+`;
- добавлены focus-эффекты для поиска;
- нижняя навигация стала мягче при переключениях;
- добавлена поддержка `prefers-reduced-motion`.

Замени:

```text
frontend/src/App.jsx
frontend/src/style.css
```

Потом перезапусти:

```bash
cd ~/Desktop/simple_shop_bot/frontend
npm run dev
```
